# 构建文件说明

## 图标文件

请将应用程序图标文件命名为 `icon.ico` 并放在此目录下。

图标要求：
- 格式：ICO
- 推荐尺寸：256x256 像素（包含多个尺寸的 ICO 文件更佳）
- 文件名：`icon.ico`

如果没有图标文件，打包时可能会显示警告，但不影响打包功能。

## 安装脚本

`installer.nsh` 文件用于配置 Windows 安装程序，包括：
- 文件关联（.md 文件）
- 右键菜单项（使用 md-edit 打开、新建 Markdown 文件）

